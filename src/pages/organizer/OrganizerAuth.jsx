import React, { useState } from 'react';\nimport { useLocation, useNavigate } from 'react-router-dom';\nimport AuthModal from '../../components/organizer/AuthModal';\n\nexport const OrganizerAuth = () => {\n  const location = useLocation();\n  const navigate = useNavigate();\n  const isSignIn = location.pathname.includes('signin') || location.pathname.includes('login');\n\n  return (\n    <div className=\"organizer-auth-page-shell page-container flex items-center justify-center p-4\">\n      <AuthModal\n        isOpen={true}\n        initialMode={isSignIn ? 'signin' : 'signup'}\n        onClose={() => navigate('/organizer')}\n      />\n    </div>\n  );\n};\n\nexport default OrganizerAuth;
